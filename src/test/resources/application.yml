#kafka:
  #bootstrap-servers: ${spring.embedded.kafka.brokers} 
  
# //to run in local point this to localhost and in test file comment the @EmbeddedKafka

#properties for main test case 

#spring:
  #kafka:
    #consumer:
      #auto-offset-reset: earliest
      
#kafka:
  #bootstrap-servers: localhost:9092 
  
#test:
  #topic: helloworld.t
  
  
#properties for sender and reciever test cases
  
kafka:
  bootstrap-servers: localhost:9092 
  topic:
    receiver: receiver.t